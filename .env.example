TELEGRAM_BOT_TOKEN=
COMFY_HOST=127.0.0.1:8188
ALLOWED_USER_IDS=123456789,987654321   # comma-separated Telegram user IDs allowed to use the bot (empty = nobody)
WORKFLOWS_DIR=./workflows              # where <name>.json / <name>.config.json pairs live
DEFAULT_WORKFLOW=krea-image            # workflow selected before a user picks one (optional)
MAX_CONCURRENT_JOBS=1
GENERATION_TIMEOUT_MS=300000
